const wallPaintColorAndSide = function (color, side) {
    console.log("The "+ side, "wall has been painted "+ color);
};

const color = "red";
const side = "north";

wallPaintColorAndSide(color, side);
//* Multiple arguments
